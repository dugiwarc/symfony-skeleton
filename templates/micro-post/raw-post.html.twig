<div class="media text-muted pt-3">
    <img src="https://i.pravatar.cc/50" alt="" class="mr-2 rounded">
    <p class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
        <span class="d-block"><strong class="text-gray-dark">@{{ post.user.username }}</strong> <small>{{ post.time|date("d/m/Y") }}</small></span>
        {{ post.text }}
    </p>
</div>